import * as React from 'react';
import * as HankLiuIcons from '@hankliu/icons/lib/icons';

const LoadModules = () => {
  React.useEffect(() => {
    console.log(HankLiuIcons);
  }, []);

  return null;
}

export default LoadModules;
